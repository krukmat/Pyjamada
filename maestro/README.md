# Maestro

Pyjamada uses one visual tour: `maestro/screenshots.yaml`.

Run it through the repository wrapper:

```bash
npm run screenshots:android
```

The flow exercises the single active gameplay path from main menu through success and restart. Do not add separate Classic/prototype flows; new visual states should extend this tour when they become part of the active game.
